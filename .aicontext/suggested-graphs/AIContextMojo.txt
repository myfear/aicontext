# Suggested @aicontext-graph for this class.
# Review and copy the block below into class-level Javadoc.
# Add [calls], [db], [events], [by] as needed.

AIContextMojo
  ├─[uses]→ AIContextEntry, FileDefinition, FilterDefinition, GraphEdge, GraphNode, ScaffoldingConfig, SortDefinition, TagData, TemplateEngine
  # Add more edges: [calls], [db], [events], [by]←, [external], [config]
